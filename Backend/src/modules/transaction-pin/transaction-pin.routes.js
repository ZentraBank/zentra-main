const router=require("express").Router(); const c=require("./transaction-pin.controller"); const s=require("./transaction-pin.validation"); const validate=require("../../middleware/validate.middleware"); const {resolveTenantMiddleware}=require("../../middleware/tenant.middleware"); const {authenticate}=require("../../middleware/auth.middleware"); router.use(resolveTenantMiddleware,authenticate); router.get("/status",c.status); router.post("/setup",validate(s.setup),c.setup); router.post("/change",validate(s.change),c.change); module.exports=router;
