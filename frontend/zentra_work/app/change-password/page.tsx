"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { authService } from "@/services/auth.service"; import { useAuthStore } from "@/store/auth.store";
export default function ChangePasswordPage(){const router=useRouter();const clearUser=useAuthStore(s=>s.clearUser);const [current,setCurrent]=useState("");const [next,setNext]=useState("");const [confirm,setConfirm]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(next!==confirm){setError("Passwords do not match");return}setLoading(true);setError("");try{await authService.changePassword(current,next);clearUser();router.replace("/login?passwordChanged=1")}catch(err){setError(err instanceof Error?err.message:"Password change failed")}finally{setLoading(false)}};return <main className="min-h-screen bg-[#e6edf6] px-4 py-24"><section className="mx-auto max-w-[430px] rounded-2xl bg-white p-6 shadow-lg"><Link href="/settings">← Back</Link><h1 className="mt-6 text-center text-2xl font-bold">Change Password</h1><form onSubmit={submit} className="mt-8 space-y-4"><input type="password" value={current} onChange={e=>setCurrent(e.target.value)} placeholder="Current password" className="h-12 w-full border-b px-2"/><input type="password" value={next} onChange={e=>setNext(e.target.value)} placeholder="New password" className="h-12 w-full border-b px-2"/><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm new password" className="h-12 w-full border-b px-2"/>{error&&<p className="text-sm text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-lg bg-[#2458E8] py-3 text-white">{loading?"Changing...":"Change password"}</button></form></section></main>}
