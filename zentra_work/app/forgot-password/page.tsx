"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { authService } from "@/services/auth.service";
export default function ForgotPasswordPage(){const router=useRouter();const [email,setEmail]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError("");try{const r=await authService.forgotPassword(email);sessionStorage.setItem("zentra_reset_email",email);if(r.developmentCode)sessionStorage.setItem("zentra_reset_code",r.developmentCode);router.push("/forgot-password/otp");}catch(err){setError(err instanceof Error?err.message:"Request failed")}finally{setLoading(false)}};return <main className="min-h-screen bg-[#E8EEF3] px-4 py-16"><section className="mx-auto max-w-[430px] rounded-2xl bg-white p-6 shadow-xl"><h1 className="text-center text-4xl font-bold text-[#555]">Forgot Password</h1><p className="mt-4 text-sm">Enter your registered email address.</p><form onSubmit={submit} className="mt-6"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="example@gmail.com" className="h-12 w-full border-b px-2 outline-none"/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-lg bg-[#2458E8] py-3 text-white">{loading?"Sending...":"Send reset code"}</button></form><Link href="/login" className="mt-5 block text-center text-sm text-[#2458E8]">Back to login</Link></section></main>}
